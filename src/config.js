import * as dotenv from 'dotenv'
import jwt from 'jsonwebtoken'
dotenv.config()

/* como todos los modulos se cargan
antes de correr codigo, ponemos
en este archivo lo del config()
para que siempre se ejecute antes
de todo */
